# Optional local environment variables.
# Do not put real secrets in committed files.

WANDB_PROJECT=r1-grpo-kaggle
WANDB_API_KEY=
WANDB_MODE=online

# Kaggle API credentials are normally stored outside the repo:
# macOS/Linux: ~/.kaggle/kaggle.json
# Kaggle runtime: use Kaggle secrets or environment variables.
